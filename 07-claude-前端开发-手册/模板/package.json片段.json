{
  "_说明": "前端项目的 scripts 和 lint-staged 配置片段。把 scripts/lint-staged 合进你的 package.json。命令标准化后,AI 的验证指令(跑 test/lint)就简单可靠,且 CI 和本地用同一套命令。",

  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test"
  },

  "lint-staged": {
    "*.{js,jsx,ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{css,json,md}": ["prettier --write"]
  },

  "_提示": "配合 husky 或 pre-commit 框架,在 git commit 时跑 lint-staged(只对暂存文件,快),对所有人生效(第5章)。husky 安装: npx husky init 然后在 .husky/pre-commit 写 npx lint-staged"
}
